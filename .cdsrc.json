{
    "requires": {
        "db": {
            "kind": "sqlite",
            "credentials": {
                "database": "db.sqlite"
            }
        },
        "auth": {
            "kind": "mocked"
        }
    }
}
